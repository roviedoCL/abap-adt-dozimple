version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    ignore:
      # zod 4 no está listo: gen-docs usa internos de zod 3 y el schema MCP pierde
      # additionalProperties:false. Quitar al cerrar el issue #4 del repositorio.
      - dependency-name: zod
        update-types: ["version-update:semver-major"]
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
