name: CI

on:
  push:
    branches: [main]
  pull_request:

# Mínimo privilegio: el workflow solo lee el repositorio.
permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      # Acciones fijadas por commit (no por etiqueta) para evitar cambios silenciosos en la cadena de suministro.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: npm
      - run: npm ci --ignore-scripts
      - run: npm test
      - run: npm run build
      - name: Auditoría de dependencias de producción
        run: npm audit --omit=dev --audit-level=moderate
      - name: Escáner de secretos (patrones genéricos; los términos de clientes solo existen en local)
        run: node scripts/scan-sensitive.mjs
      - name: La documentación generada está al día
        run: node dist/scripts/gen-docs.js && git diff --exit-code -- README.md docs/TOOLS.md
